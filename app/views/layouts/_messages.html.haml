- flash.each do |name, msg|
  - if msg.is_a?(String)
    = content_tag :div, data: 'alert', class: "alert-box round #{name.to_s == 'notice' ? 'success' : 'alert'}" do
      = content_tag :div, msg
      = link_to "X", "#", class: "close"